import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aurabrowser.app',
  appName: 'Aura Browser',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
